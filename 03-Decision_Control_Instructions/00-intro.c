/* Decision Control INstructions */
/*
    In C language there are three satement which is for use for decision control instrucation

    1. if statement
    2. if-else statement
    3. ladder if statement


        Operators               Description
           ==              Equal to (e.g : x == y)
           !=              Not equal to (e.g : x != y)
            >              Greater than (e.g : x == y)
            <              Less than (e.g : x == y)
           >=              Greater than equal to (e.g : x == y)
           >=               Less than equal to (e.g : x == y)
           ==              Equal to (e.g : x == y)
*/