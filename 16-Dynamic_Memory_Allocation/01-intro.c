/* Dynamic Memory Allocation */
/*
    In C language dynamic memory allocation is refers to allocate memory space at runtime, which allowing the programmer to manage memory dynamically it very useful when you don't know how many memory space are required at compile time & it is allocate memory on the base of user input or other runtime condition.

    'malloc() calloc(), realloc() and free() are the function which is use for the dynamic memory to allocation and it available in the <stdlib.h> header file.
*/